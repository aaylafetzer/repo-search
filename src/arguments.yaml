name: reposearch
version: "0.0.1"
author: Aayla Fetzer <aayla.fetzer@gmail.com>
about: Searches git repositories for configurable regular expressions
args:
  - EXPRESSIONS:
      help: Path to the regular expressions to search for
      required: true
      index: 1
  - REPO:
      help: Path to the git repository
      required: true
      index: 2